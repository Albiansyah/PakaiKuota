Sudah dikonfirmasi via `git log --oneline -5`: commit b55f048 adalah HEAD
lokal DAN origin/main DAN commit yang sama persis dibuild Vercel. Tidak
ada perbedaan source tree, tidak ada monorepo/root directory issue, tidak
ada branch mismatch — semua identik.

Jadi hipotesis "source berbeda" itu SALAH, hentikan investigasi ke arah
itu. Diagnosis awal sebelum ini (missing dependencies + naming mismatch
createClient/createSupabaseServerClient) yang BENAR dan perlu diperbaiki
sekarang — kemungkinan besar package.json belum ter-update dengan benar
saat commit "Merge remote main with local implementation" (b8c51de),
menyebabkan dependency baru (dropdown-menu, label, slot, switch, tabs,
class-variance-authority, clsx, lucide-react, tailwind-merge) tidak
tercatat meski filenya sudah ada di commit.

Lanjutkan fix sesuai rencana sebelumnya:
1. npm install untuk semua package yang hilang, verifikasi package.json
   ter-update dengan benar
2. Tambahkan alias export di src/lib/supabase/server.ts:
   export { createSupabaseServerClient as createClient }
3. Jalankan npm run build LOKAL, pastikan sukses tanpa error
4. Baru commit & push setelah build lokal terbukti hijau